{
  "name": "ninja-kitten-team-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node scripts/server.mjs",
    "check": "node --check src/domain.js && node --check src/tag-rules.js && node --check src/missions.js && node --check src/mission-store.js && node --check src/app.js && node --check scripts/server.mjs",
    "test": "node --test",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
